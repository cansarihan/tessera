# Tessera API configuration (testnet defaults are used when unset)

PORT=8788
CORS_ORIGIN=*

# On-chain target (defaults read from /deployments/testnet.json)
CONTRACT_ID=CBPKPVNYRW6HS7ZZYAB6QZ45FHJTA2XWJBAPWTV2R5UBED4SCROPMLFG
RPC_URL=https://soroban-testnet.stellar.org
NETWORK_PASSPHRASE=Test SDF Network ; September 2015
NETWORK=testnet

# Database — local file in dev; Turso (libSQL) in production for persistence
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=...

# Indexer
INDEXER_ENABLED=true
INDEXER_INTERVAL_MS=5000
INDEXER_LOOKBACK_LEDGERS=2000
